package githubql

/*
// RepositoryArguments are arguments for Query.Repository field.
type RepositoryArguments struct {
	Owner String // The login field of a user or organization. (Required.)
	Name  String // The name of the repository. (Required.)
}

// IssueArguments are arguments for Repository.Issue field.
type IssueArguments struct {
	Number Int // The number for the issue to be returned. (Required.)
}

// CommentsArguments are arguments for Issue.Comments field.
type CommentsArguments struct {
	First  *Int    // Returns the first n elements from the list. (Optional.)
	After  *String // Returns the elements in the list that come after the specified global ID. (Optional.)
	Last   *Int    // Returns the last n elements from the list. (Optional.)
	Before *String // Returns the elements in the list that come before the specified global ID. (Optional.)
}
*/
